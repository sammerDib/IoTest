namespace UnitySC.Shared.FDC.Interface
{
    public static class FDCsConsts
    {
        // Heartbeat delay in milliseconds to notifiy the server that the client is still alive
        public const int HeartBeatDelay_ms = 5000;
    }
}
