namespace UnitySC.PM.Shared.Hardware.Camera.MatroxCamera
{
    /// <summary>
    /// Area is the standard area mode (ex: 4/3), tdi averages all lines in Y, and behaves as a line cam, with an improved noise level.
    /// </summary>
    public enum CameraMode : byte { area, tdi };
}